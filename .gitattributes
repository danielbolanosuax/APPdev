# Normaliza texto; que Git detecte/gestione automáticamente
* text=auto

# Asegura LF en scripts de shell y hooks (crítico en Windows)
*.sh           text eol=lf
.husky/*       text eol=lf

# Si usas scripts .bat en Windows, mantenlos CRLF
*.bat          text eol=crlf
